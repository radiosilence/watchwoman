{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'cmd=\"$CLAUDE_TOOL_INPUT_command\"; case \"$cmd\" in *\"git push\"*) cd \"$CLAUDE_PROJECT_DIR\" && ~/.cargo/bin/cargo fmt --all --check && ~/.cargo/bin/cargo clippy --workspace --all-targets -- -D warnings ;; esac'"
          }
        ]
      }
    ]
  }
}
